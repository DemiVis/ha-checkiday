## Summary

<!-- What does this PR change and why? -->

## Checklist

- [ ] Ran `ruff check` / `ruff format` locally
- [ ] Tested the config flow (including a deliberately invalid API key)
- [ ] Tested the options flow (update time + tomorrow toggle)
- [ ] Updated `strings.json` **and** `translations/en.json` if UI text changed
- [ ] Bumped `version` in `manifest.json` if this is a release-worthy change
